name: CI (Release)
on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest

    outputs:
      artifact-id: ${{ steps.upload-artifact.outputs.artifact-id }}

    steps:
    - uses: actions/checkout@v7
      with:
        submodules: true

    - uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '6.0.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build -c Release --no-restore

    - name: Publish
      run: dotnet publish -p:PublishSingleFile=true -p:CommitHash=${{ github.sha }} -p:CommitRef=${{ github.ref_type }}/${{ github.ref_name }} -r win-x64 -c Release --self-contained false .\RailStrap\RailStrap.csproj

    - name: Upload Artifact
      id: upload-artifact
      uses: actions/upload-artifact@v7
      with:
        name: RailStrap (Release) (${{ github.sha }})
        path: .\RailStrap\bin\Release\net6.0-windows\win-x64\publish\*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write
      actions: read

    steps:
      # NOTE: unsigned build - RailStrap does not have its own code-signing certificate/SignPath
      # account yet, so Windows SmartScreen will warn on first run. Set one up here if that changes.
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: RailStrap (Release) (${{ github.sha }})
          path: release
          run-id: ${{ github.run_id }}
          github-token: ${{ github.token }}

      - name: Rename binaries
        run: mv release/RailStrap.exe RailStrap-${{ github.ref_name }}.exe

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          generate_release_notes: true
          files: RailStrap-${{ github.ref_name }}.exe
          name: RailStrap ${{ github.ref_name }}

  release-test:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/release-test')

    permissions:
      contents: write
      actions: read

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: RailStrap (Release) (${{ github.sha }})
          path: release
          run-id: ${{ github.run_id }}
          github-token: ${{ github.token }}

      - name: Rename binaries
        run: mv release/RailStrap.exe RailStrap-${{ github.ref_name }}.exe

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          files: RailStrap-${{ github.ref_name }}.exe
          name: RailStrap ${{ github.ref_name }}
