<base:WpfUiWindow x:Class="RailStrap.UI.Elements.Dialogs.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:RailStrap.UI.Elements.Dialogs"
        xmlns:base="clr-namespace:RailStrap.UI.Elements.Base"
        xmlns:resources="clr-namespace:RailStrap.Resources"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.LogViewer_Title}"
        MinWidth="480"
        MinHeight="320"
        Width="900"
        Height="600"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" x:Name="RootTitleBar" Title="{x:Static resources:Strings.LogViewer_Title}" Icon="pack://application:,,,/RailStrap.ico" MinimizeToTray="False" UseSnapLayout="True" />

        <avalonedit:TextEditor
            x:Name="UIXML"
            Grid.Row="1"
            Margin="10,10,10,10"
            ShowLineNumbers="True"
            IsReadOnly="True"
            WordWrap="False"
            Style="{StaticResource NewTextEditor}" />
    </Grid>
</base:WpfUiWindow>
