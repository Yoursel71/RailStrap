<base:WpfUiWindow x:Class="RailStrap.UI.Elements.Dialogs.FriendActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:RailStrap.UI.Elements.Base"
        xmlns:models="clr-namespace:RailStrap.UI.ViewModels.Dialogs"
        xmlns:resources="clr-namespace:RailStrap.Resources"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance models:FriendActivityViewModel, IsDesignTimeCreatable=True}"
        Title="{x:Static resources:Strings.Menu_FriendActivity_Title}"
        Width="360"
        MinHeight="0"
        MaxHeight="500"
        SizeToContent="Height"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" Title="{x:Static resources:Strings.Menu_FriendActivity_Title}" />

        <ui:ProgressRing Grid.Row="1" Margin="0,24,0,24" IsIndeterminate="True" Visibility="{Binding LoadingVisibility}" />

        <TextBlock Grid.Row="1" Margin="16" TextWrapping="Wrap" Text="{Binding ErrorMessage}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding ErrorVisibility}" />

        <ScrollViewer Grid.Row="1" MaxHeight="420" Visibility="{Binding ContentVisibility}">
            <ItemsControl Margin="12" ItemsSource="{Binding Friends}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding Status}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</base:WpfUiWindow>
