<ui:UiPage x:Class="RailStrap.UI.Elements.Settings.Pages.PluginsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:RailStrap.UI.ViewModels.Settings"
      xmlns:resources="clr-namespace:RailStrap.Resources"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance models:PluginsViewModel, IsDesignTimeCreatable=True}"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="PluginsPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="{x:Static resources:Strings.Menu_Plugins_Description}" TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <Grid Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static resources:Strings.Menu_Plugins_Title}" FontSize="20" FontWeight="Medium" VerticalAlignment="Center" />
            <ui:Button Grid.Column="1" Margin="0,0,8,0" Icon="ArrowImport20" Content="{x:Static resources:Strings.Common_Import}" Command="{Binding ImportCommand, Mode=OneTime}" />
            <ui:Button Grid.Column="2" Margin="0,0,8,0" Icon="ArrowClockwise24" Content="{x:Static resources:Strings.Common_Refresh}" Command="{Binding RefreshCommand, Mode=OneTime}" />
            <ui:Button Grid.Column="3" Icon="Folder24" Content="{x:Static resources:Strings.Common_OpenFolder}" Command="{Binding OpenFolderCommand, Mode=OneTime}" />
        </Grid>

        <TextBlock Margin="0,24,0,0" HorizontalAlignment="Center" Text="{x:Static resources:Strings.Menu_Plugins_Empty}" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding EmptyVisibility}" />

        <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding Plugins}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ui:CardControl Margin="0,4,0,0">
                        <ui:CardControl.Icon>
                            <ui:SymbolIcon Symbol="PuzzleCube24" />
                        </ui:CardControl.Icon>
                        <ui:CardControl.Header>
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                        </ui:CardControl.Header>
                        <StackPanel Orientation="Horizontal">
                            <ui:ToggleSwitch IsChecked="{Binding Enabled, Mode=OneWay}" Command="{Binding DataContext.ToggleEnabledCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                            <ui:Button Margin="8,0,0,0" Icon="Delete24" Appearance="Danger" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                        </StackPanel>
                    </ui:CardControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</ui:UiPage>
