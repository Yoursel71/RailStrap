<ui:UiPage x:Class="RailStrap.UI.Elements.Settings.Pages.GalleryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:RailStrap.UI.ViewModels.Settings"
      xmlns:config="clr-namespace:RailStrap.Models.APIs.Config"
      xmlns:resources="clr-namespace:RailStrap.Resources"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance models:GalleryViewModel, IsDesignTimeCreatable=True}"
      d:DesignHeight="800" d:DesignWidth="800"
      Title="GalleryPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="{x:Static resources:Strings.Menu_Gallery_Description}" TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <ui:ProgressRing Margin="0,32,0,0" IsIndeterminate="True" Visibility="{Binding LoadingVisibility}" />

        <TextBlock Margin="0,16,0,0" HorizontalAlignment="Center" TextWrapping="Wrap" Text="{x:Static resources:Strings.Menu_Gallery_LoadFailed}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding LoadFailedVisibility}" />

        <StackPanel Visibility="{Binding ContentVisibility}">
            <TextBlock Text="{x:Static resources:Strings.Menu_Gallery_Themes}" FontSize="20" FontWeight="Medium" Margin="0,16,0,0" />
            <ItemsControl ItemsSource="{Binding Themes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type config:GalleryItem}">
                        <ui:CardExpander Margin="0,4,0,0" IsExpanded="False">
                            <ui:CardExpander.Icon>
                                <ui:SymbolIcon Symbol="PaintBrush24" />
                            </ui:CardExpander.Icon>
                            <ui:CardExpander.Header>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" />
                                    <TextBlock Text="{Binding Author}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                </StackPanel>
                            </ui:CardExpander.Header>
                            <StackPanel Margin="0,8,0,0">
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                <ui:Button Margin="0,8,0,0" Content="{x:Static resources:Strings.Menu_Gallery_Install}" Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=ui:UiPage}}" CommandParameter="{Binding}" />
                            </StackPanel>
                        </ui:CardExpander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{x:Static resources:Strings.Menu_Gallery_Mods}" FontSize="20" FontWeight="Medium" Margin="0,24,0,0" />
            <ItemsControl ItemsSource="{Binding Mods}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type config:GalleryItem}">
                        <ui:CardExpander Margin="0,4,0,0" IsExpanded="False">
                            <ui:CardExpander.Icon>
                                <ui:SymbolIcon Symbol="WrenchScrewdriver24" />
                            </ui:CardExpander.Icon>
                            <ui:CardExpander.Header>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" />
                                    <TextBlock Text="{Binding Author}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                </StackPanel>
                            </ui:CardExpander.Header>
                            <StackPanel Margin="0,8,0,0">
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                <ui:Button Margin="0,8,0,0" Content="{x:Static resources:Strings.Menu_Gallery_Install}" Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=ui:UiPage}}" CommandParameter="{Binding}" />
                            </StackPanel>
                        </ui:CardExpander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </StackPanel>
</ui:UiPage>
