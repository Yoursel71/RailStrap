<base:WpfUiWindow
    x:Class="RailStrap.UI.Elements.Bootstrapper.VideoDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:RailStrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:RailStrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vms="clr-namespace:RailStrap.UI.ViewModels.Bootstrapper"
    Width="480"
    Height="270"
    MinHeight="0"
    d:DataContext="{d:DesignInstance vms:BootstrapperDialogViewModel,
                                     IsDesignTimeCreatable=True}"
    Background="Black"
    Closing="UiWindow_Closing"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="None"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressState="{Binding Path=TaskbarProgressState}" ProgressValue="{Binding Path=TaskbarProgressValue}" />
    </Window.TaskbarItemInfo>

    <Grid>
        <MediaElement
            x:Name="LoadingVideo"
            LoadedBehavior="Manual"
            MediaEnded="LoadingVideo_MediaEnded"
            Stretch="UniformToFill"
            UnloadedBehavior="Manual"
            Volume="0" />

        <!--  Invisible drag handle  -->
        <ui:TitleBar
            VerticalAlignment="Top"
            Background="Transparent"
            CanMaximize="False"
            ShowClose="False"
            ShowMaximize="False"
            ShowMinimize="False" />

        <ui:Button
            Margin="0,8,8,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Appearance="Transparent"
            Command="{Binding CancelInstallCommand}"
            Content="{x:Static resources:Strings.Common_Cancel}"
            Foreground="White"
            Visibility="{Binding CancelButtonVisibility, Mode=OneWay}" />
    </Grid>
</base:WpfUiWindow>
